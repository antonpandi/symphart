{% extends 'base.html.twig' %}

{% block title %} {{article.title}} {% endblock %}

{% block body %}


 <h1>{{article.title}}</h1>
 <p>{{article.body}}</p>
 <p>Author <a href="{{article.username ? "/user/" ~ article.username : '/'}}"> {{article.username ?: 'Anonymous'}} <a> </p>
 <hr>
 <a href="/">Go back</a>
{% endblock %}